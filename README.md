# Entity Migrator

Framework to migrate Doctrine entities in the background: migrations are queued through symfony/messenger and tracked with a workflow, with commands to set up and run them.

## Installation

```bash
composer require codraw/entity-migrator
```
